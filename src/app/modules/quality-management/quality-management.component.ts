import { Component } from '@angular/core';

/**
 * Review page for the data quality record of the active study: the criteria sets that define
 * fitness for use, and the assessment runs that checked the datasets against them.
 */
@Component({
    selector: 'app-quality-management',
    templateUrl: './quality-management.component.html'
})
export class QualityManagementComponent {
}
