import { Component } from '@angular/core';

/**
 * The Data Steward's page: the datasets their organization owns, and the metadata they complete
 * before those datasets can be published.
 */
@Component({
    selector: 'app-steward-management',
    templateUrl: './steward-management.component.html'
})
export class StewardManagementComponent {
}
